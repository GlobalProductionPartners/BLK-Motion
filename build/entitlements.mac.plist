<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    Hardened Runtime exceptions BLK Motion genuinely needs.
    Not sandboxed (a show-control app must reach the network and USB freely),
    so no sandbox entitlements here.
  -->

  <!-- V8 compiles JavaScript at runtime -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>

  <!-- Electron sets DYLD_* when launching helper processes -->
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>

  <!-- serialport ships a prebuilt native binding (USB-DMX widgets) -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
